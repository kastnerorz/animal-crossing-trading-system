export default function ({$axios, redirect, $buefy}) {
    // $axios.onRequest(config => {

    // })

    // $axios.onError(error => {
    //     $buefy.toast.open({
    //         message: 's',
    //         type: 'is-danger'
    // })
    // })
}